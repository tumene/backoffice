import { TransactionApprovalSearchPipe } from './transaction-approval-search.pipe';

describe('TransactionApprovalSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionApprovalSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
